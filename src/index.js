import React from 'react';
import ReactDOM from 'react-dom/client';
import LP from './LadingPage';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <LP />
  </React.StrictMode>
);
